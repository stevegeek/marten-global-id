/docs/
/lib/
/bin/
/.shards/
*.dwarf
.DS_Store

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Gitignored — workspace path-deps overlay for local development.
shard.override.yml
